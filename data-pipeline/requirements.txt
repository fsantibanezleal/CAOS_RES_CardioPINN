# OFFLINE pipeline (heavy, local GPU). Install into .venv-pipeline. The bake runs on a local NVIDIA GPU;
# the derived artifacts (traces + catalogue + manifests) are committed, so neither CI nor the web ever runs
# this stack. Keep this file in coherence with the REAL imports of data-pipeline/cardiopinnlab (numpy, scipy,
# torch, pydicom); nothing here should be a dependency the code does not actually import.
#
# torch: install the CUDA build first for the GPU (RTX-class), then the rest:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r data-pipeline/requirements.txt
# (the CPU wheel also works; the cardiac PINNs are small MLPs that train in minutes.)
torch>=2.5                  # 4D-flow: divergence-free velocity PINN (denoise) + the Navier-Stokes pressure engine
numpy>=2.0,<2.3
scipy>=1.13                 # ECGi reconstruction (linear algebra), 4D-flow sparse pressure-Poisson solve + ndimage
pydicom>=3.0                # decode the real 4D-flow MRI DICOM velocity series (cardiopinnlab/real/flow4d_dicom.py)
